import { Routes } from '@angular/router';
import { NetworkErrorComponent } from 'app/modules/error/network-error/network-error.component';

export default [{ path: '', component: NetworkErrorComponent }] as Routes;
